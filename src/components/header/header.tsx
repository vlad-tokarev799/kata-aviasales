import React from 'react';

import { StyledHeader } from './header.styles';
import { Logo } from '../logo/logo';

export const Header = () => {
  return (
    <StyledHeader>
      <Logo />
    </StyledHeader>
  );
};
